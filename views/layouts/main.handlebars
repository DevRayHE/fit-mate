<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fit-Mate</title>
    <link rel="stylesheet" href="/css/jass.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="flex-column justify-space-around">
        <header class="display-flex justify-space-between align-center p-5 bg-primary bg-opacity-50">
            <p><a href="/">
            <img src="/css/logo.png" class="rounded float-start">
            </a>
            </p>
            <nav class=" d-flex justify-content-around pe-5">
                
                {{!-- {{#if logged_in}} --}}
                <div class=" d-flex px-2">
                    <a class="btn btn-primary" href="/dashboard">DASHBOARD</a>
                </div>
                <div class=" d-flex px-2">
                    <a class="btn btn-primary" href="/leaderboard">LEADERBOARD</a>
                </div>
                {{!-- {{/if}} --}}

                <div class="d-flex px-2">
                    <!-- Conditionally render login or logout links -->
                    {{#if logged_in}}
                    <button class="btn btn-primary" id="logout">LOGOUT</button>
                    {{else}}
                    <a class="btn btn-primary" href="/login">LOGIN</a>
                    {{/if}}

                </div>

            </nav>
        </header>
        <main class="container container-fluid text-center my-5">
            <!-- Render the sub layout -->
            {{{ body }}}
        </main>
        <footer class="bg-primary bg-opacity-50 p-5 " style="width: 100%;">
            <div class="container container-fluid">
                <div class="row">
                    <p class="copyright-text">
                        Copyright &copy; 2021 </br>
                        All Rights Reserved by
                        <a href="#">FitMate</a>.
                    </p>
                </div>
            </div>
        </footer>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>