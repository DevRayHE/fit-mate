{{!-- Render the form based on whether it's for new blog or edit blog
<div class="container">
  <h2>
    {{#if newExerise}}
    Add an exercise
    {{/if}}
    {{#if editExercise}}
    Edit Exercise
    {{/if}}
  </h2>

  {{#if newExerise}}
  <form class="form newBlog-form">
  {{/if}}
  {{#if editExercise}}
  <form class="form updateBlog-form" data-id="{{id}}">
  {{/if}}
    <div class="form-group">
      <label for="titlet">Title</label>
      <input class="form-input" type="text" id="title"
      {{#if editExercise}}
      value="{{title}}"
      {{/if}}
      >
    </div>
    <div class="form-group">
      <label for="content">Content</label>
      <textarea class="form-input" type="text" id="content" rows="5" cols="50">
        {{#if editBlog}}
        {{content}}
        {{/if}}
      </textarea>
      
    </div>
    {{#if newBlog}}
    <div class="form-group">
      <button class="btn btn-success" type="submit">
        Post new blog
      </button>
    </div>
    {{/if}}

    {{#if editBlog}}
    <div class="flex-row">
      <div class="form-group">
        <button class="btn btn-success" type="submit">
          Update Blog
        </button>
      </div>

      <button class="btn btn-danger" data-id="{{blog.id}}" id="del-btn">
        Delete
      </button>
    </div>
    {{/if}}
  </form>
</div>

{{#if newBlog}}
<script src="/js/newBlog.js"></script>
{{/if}}

{{#if editBlog}}
<script src="/js/updateBlog.js"></script>
{{/if}}



{{!-- // Creating the form with checkbox selection, limit to 1 selection only? --}}
<form>
  <input type="checkbox" id="dance" name="dance" value="Dance">
  <label for="dance"> Dance (Indoor)</label><br>
  <input type="checkbox" id="yoga" name="yoga" value="Car">
  <label for="yoga"> I have a car</label><br>
  <input type="checkbox" id="swimming" name="vehicle3" value="Boat">
  <label for="vehicle3"> I have a boat</label>
</form> --}}